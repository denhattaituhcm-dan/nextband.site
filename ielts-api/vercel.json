{
  "buildCommand": "cd nextband && npm install && npm run build",
  "outputDirectory": "nextband/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
